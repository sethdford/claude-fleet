# =============================================================================
# Claude Fleet — Environment Variables
# =============================================================================
# Copy to .env and fill in values for local development.

# Server
PORT=3847
HOST=0.0.0.0
NODE_ENV=development

# Authentication
JWT_SECRET=change-me-to-a-random-secret

# Anthropic API (for agent spawning)
ANTHROPIC_API_KEY=sk-ant-...

# Webhook integrations (optional)
GITHUB_WEBHOOK_SECRET=
SLACK_WEBHOOK_URL=
TEAMS_WEBHOOK_URL=
DISCORD_WEBHOOK_URL=

# Storage backend: sqlite (default) | dynamodb | postgres
STORAGE_BACKEND=sqlite
DATABASE_PATH=./data/fleet.db
